FOUNDATION / 28

FILES
- index.html: the entire workout app (HTML/CSS/JS, no external dependencies).
- manifest.webmanifest + sw.js: optional Progressive Web App files for HTTPS hosting.

DEFAULT PROGRAM START
August 20, 2026. You can change this in Settings.

EASIEST TEST
Open index.html in a modern browser. Progress is stored in that browser's localStorage.

BEST IPHONE EXPERIENCE
Serve the folder over HTTPS as a static website (for example, GitHub Pages), open it in Safari,
then use Share > Add to Home Screen. Once loaded, the service worker can cache the app for offline use.

IMPORTANT
The app is a workout timer/logger, not a medical device. Stop exercise for warning symptoms or significant pain.
